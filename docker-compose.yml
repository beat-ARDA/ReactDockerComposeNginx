version: '3.7'

services:
  web:
    container_name: web
    build:
      context: ./web
      dockerfile: Dockerfile
    expose:
      - 3000  
    volumes:
      - './web:/app'
      - '/app/node_modules'
    ports:
      - '3000:3000'
      